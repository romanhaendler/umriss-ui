export { Popover } from "./Popover";
export type { PopoverProps } from "./Popover";

/* computePosition and its types stay internal on purpose: they are the test
   seam of the primitive, not part of what the package offers. */
