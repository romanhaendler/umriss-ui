export default function WhyTag() {
  return (
    <>
      <h3>The tone is never the only information</h3>
      <p>
        Beside every label stands its word. Whoever cannot distinguish the colours reads the same
        here as everybody else - and whoever can distinguish them reads it faster. That is not a
        retrofit for accessibility but the condition under which a colour is allowed to mean
        anything at all.
      </p>

      <h3>Contrast in both themes</h3>
      <p>
        The five tones are checked against their own background, light and dark, against WCAG 2.1
        AA. A tone that misses the threshold in one of the two themes is not a tone but a defect
        that only half the users notice.
      </p>

      <h3>Disabled means: no button</h3>
      <p>
        A disabled label keeps its place and loses its remove button. Disabled is a statement about
        the button and not about the label - a label that disappeared would be different
        information.
      </p>
    </>
  );
}
