export default function WhyControlRoom() {
  return (
    <>
      <h3>One plant, not six data sets</h3>
      <p>
        Every part of the room reads the same seeded shift (<code>plant.ts</code>, in the second tab of
        each example). That is the claim of the page: the kiln crossing its limit is one fact, and the
        trend, the tile, the alarm list, the control chart and the OEE each find it by reading the same
        minute against the same limits. Six data sets tuned to agree would prove nothing - they would
        agree because somebody made them.
      </p>

      <h3>A page here, not a sixth demo</h3>
      <p>
        The core demo is the one a reader opens first, and a consumer combines the packages exactly
        like this: core underneath, the others beside it. So the core demo is the one demo that may
        import every package - by alias, as the other demos reach core, and only in its demo: the lint
        keeps the packages&apos; sources to their own directions.
      </p>

      <h3>A clock that stands still when it should</h3>
      <p>
        The shift runs a plant minute per second, counted from what the wall clock says has passed -
        so a frozen clock is a still page, which is what makes the screenshots hold. Under reduced
        motion it starts paused, and it can always be paused: a page that updates on its own for
        minutes owes its reader a way to stop it.
      </p>

      <h3>Every part keeps its own keyboard</h3>
      <p>
        The page adds nothing to the parts&apos; keyboard models. It adds a landmark per region and a
        row of skip links that move the focus there - not the address, which belongs to the demo.
      </p>
    </>
  );
}
