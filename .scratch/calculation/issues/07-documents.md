# 07 — The documents follow

Status: ready-for-agent
Type: task

Blocked by: 06
Spec: "Package", "Further Notes"

## Scope

- `CLAUDE.md` (five packages become six; the dependency sentence), the opening
  paragraph of `CONTEXT.md` (it still names three packages), root `README.md`,
  `CONTRIBUTING.md`, `docs/README.md`, `docs/testing.md`, `docs/releasing.md`,
  ADR-0020's count of demos as a dated addendum, the shell's descriptions.
- The package's `README.md` and `CHANGELOG.md`; an unreleased entry in core's
  changelog for the new wording entries; `docs/journal.md`.
- The spec's status to `done` with a delivery report under `## Comments`.
