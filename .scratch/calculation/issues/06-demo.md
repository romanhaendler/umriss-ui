# 06 — The demo

Status: done
Type: task

Blocked by: 05
Spec: "Solution", "Package"

## Scope

- Pages and examples on the shared shell: OEE as the lead example; one with a
  missing given; one with an approximation mark; one built from data with
  `.map` (cost per piece from a list of cost items); one with explanation,
  source and freshness, and the caller's slot holding a sparkline.
- Props tables through the props gate.
- Visual baselines, light and dark.

## Acceptance

- `pnpm test:unit` green; the new pages' pictures are new baselines; the
  image and accessibility checks of the demos include the package.
